#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
VENV_PYTHON="${SCRIPT_DIR}/venv/bin/python"

if [[ -x "${VENV_PYTHON}" ]]; then
    exec "${VENV_PYTHON}" -m app.policy_cli "$@"
fi

exec python3 -m app.policy_cli "$@"
